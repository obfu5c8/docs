
These docs are one 'vault' / subset of the set of documentation maintained on [[Firebat]] and synchronised via [[Syncthing]] to other devices on the network - usually consumed via Obsidian clients.

They are also backed up hourly to a github repo (github.com/obfu5c8/docs) via a systemd service/timer on [[Firebat]].
